# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no Git!

# URL da API do backend (Railway em produção, localhost em desenvolvimento)
VITE_API_URL=http://localhost:3001
