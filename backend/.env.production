# Database (será configurado no Railway)
DATABASE_URL=${DATABASE_URL}

# Server
PORT=3000
NODE_ENV=production

# JWT
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=7d

# CORS - Vercel frontend
CORS_ORIGIN=https://meu-clube-do-livro.vercel.app
